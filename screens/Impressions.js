import React from 'react'

import {
  StyleSheet,
  View,
} from 'react-native'

const Impressions = ({ navigation }) => {

  return (
      <View style={styles.impressionsContainer}>
      </View>
  )
}

const styles = StyleSheet.create({
  impressionsContainer: {
  },
});

export default Impressions;